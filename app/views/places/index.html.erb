<h1>My Places</h1>

<ul>
  <% @places.each do |place| %>
    <li>
      <a href="<%= place_path(place) %>"><%= place.name %></a>
    </li>
  <% end %>
</ul>

<p><a href="<%= new_place_path %>">Add a New Place</a></p>
